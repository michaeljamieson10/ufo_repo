services:
  falkordb:
    image: falkordb/falkordb:latest
    container_name: ufo-falkordb
    ports:
      - "6379:6379"   # Redis-protocol port for the falkordb client
      - "13000:3000"  # FalkorDB Browser UI (open http://localhost:13000)
                      # 3000 is commonly taken by Grafana / dev servers,
                      # so we expose on 13000 to avoid the collision.
    volumes:
      # FalkorDB writes dump.rdb to /var/lib/falkordb/data, NOT /data
      # (its CONFIG dir override). Mount the persistence path directly
      # so on-host backups + tarball releases see the live RDB.
      - ./data/falkordb:/var/lib/falkordb/data
    restart: unless-stopped
